services:
  - type: web
    name: asset-app-back
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx prisma migrate deploy && node dist/src/main.js
    envVars:
      - key: NODE_ENV
        value: production
